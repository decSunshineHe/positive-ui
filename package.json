{
  "name": "positive",
  "private": true,
  "keywords": [
    "react",
    "typescript",
    "docusaurus",
    "rc-components",
    "vite"
  ],
  "scripts": {
    "release": "lerna publish --force-publish --tag-version-prefix=''",
    "release:beta": "lerna publish --dist-tag beta --tag-version-prefix=''",
    "start": "lerna run --scope website dev",
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "prepare": "husky install",
    "build": "lerna run --scope @positive-map/rc-utils build && lerna run --scope @positive-map/rc-components build",
    "clean": "rimraf packages/*/package-lock.json && rimraf website/package-lock.json && rimraf package-lock.json && rimraf packages/*/node_modules && rimraf website/node_modules && rimraf node_modules"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^6.5.1",
    "prettier": "^2.8.7"
  }
}
