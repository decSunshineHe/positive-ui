{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "npm run bootstrap",
    "bootstrap": "lerna bootstrap",
    "prepare": "husky install",
    "website": "lerna run --scope positive-website start",
    "build": "lerna run --scope positive-ui build",
    "publish": "npm run build && lerna run --scope positive-ui publish"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lerna": "^6.5.1",
    "prettier": "^2.8.7"
  }
}
